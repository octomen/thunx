version: "2"
services:
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
    ports:
      - 80:6000
    networks:
      - thupoll_default

networks:
  thupoll_default:
    external: true
