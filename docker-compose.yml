version: "2"
services:
  nginx:
    image: nginx
    network_mode: "host"
    # volumes:
      # - ./nginx.conf:/etc/nginx/conf.d/default.conf
      # - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
    environment:
     - NGINX_HOST=thupoll.liinda.ru
     - NGINX_PORT=80
    ports:
     - 80:80
